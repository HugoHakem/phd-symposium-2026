pages:
  image: node:lts
  before_script:
    - npm install -g pnpm
    - pnpm install
  script:
    - pnpm run build
  variables:
    ASTRO_SITE: 'https://phdsymposium.embl-community.io'
    ASTRO_OUT_DIR: 'public'
  artifacts:
    paths:
      - public
  only:
    - gitlab
